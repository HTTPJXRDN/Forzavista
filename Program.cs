// The WinForms entry point and MainForm were replaced by the WPF App.xaml /
// MainWindow.xaml (Forza Mods themed redesign). The application now starts from
// App.xaml (UseWPF), so this file intentionally contains no entry point.
